# S3 Filer 利用マニュアル（日本語）

起動コマンド: **`sss`**（s が 3 つ = S3）  
インストール手順は [README.md](README.md) を参照してください。

---

## 1. 概要

S3 Filer は、MS-DOS 時代の FD / FILMTN を意識した **2 ペイン型 TUI ファイラー**です。

- 左・右ペインで **ローカル** と **S3** を行き来
- コピー / 移動 / 削除 / 改名・View・編集・アーカイブ・コマンド実行・サブシェル
- **画像の SIXEL 表示**、拡張子ごとの外部ビューア、UI の日英切替
- 設定はユーザー設定ファイルに保存され、次回起動時に復元されます

---

## 2. 起動と終了

```bash
./sss                    # Linux / WSL / Git Bash
./sss -p myprofile
.\sss.ps1                # Windows PowerShell（.\sss は不可・bash 用のため）
.\sss.ps1 -p myprofile
.\sss.cmd -p myprofile   # Windows cmd
```

| キー | 動作 |
|------|------|
| **q** / **Esc** | 終了確認 |
| **F1** / **?** | ヘルプ（**表示言語に合わせて**日本語 / 英語） |
| **u** | **設定画面** |

終了確認で Yes を選ぶとアプリが閉じます。

---

## 3. 画面構成

- **タイトルバー**: バージョン、AWS プロファイル、リージョン  
  （テーマ名や F1/u のキー案内は出さず、すっきり表示）
- **左 / 右ペイン**: ヘッダは「左」「右」、ファイル一覧（`*` は複数選択）
- **メッセージ行**: 操作結果・進捗・警告
- **ファンクションバー**: 主要キーの要約（例: `h/l ペイン  j/k 上下  Spc 選択  Q 終了`）  
  テーマ変更は **u** 設定内のみ（ステータスにテーマキーは出しません）

### ナビゲーション（Vim 風）

| キー | 動作 |
|------|------|
| **h** / ← | 左ペイン |
| **l** / → | 右ペイン |
| **j** / ↓ | カーソル下 |
| **k** / ↑ | カーソル上 |
| **Tab** | ペイン切替 |
| **Enter** | ディレクトリを開く / 実行可能なら実行 / アーカイブなら一覧 / それ以外 View |
| **Backspace** | 親ディレクトリ |
| **Space** / **Insert** | 複数選択トグル |
| **\*** | ファイル全選択 / 解除 |
| **Ctrl+A** | 全選択 / 解除 |

### よく使う操作

| キー | 動作 |
|------|------|
| **c** / F5 | コピー → 反対ペイン |
| **C** | コピー → 宛先ブラウザ |
| **m** / F6 | 移動 → 反対ペイン |
| **M** | 移動 → 宛先ブラウザ |
| **v** / F3 | ファイル表示 (View) / 画像は SIXEL |
| **e**（View 内） | 外部エディタ |
| **i** / F4 | ファイル情報 |
| **r** / F2 | 名前変更 |
| **n** / F7 | ディレクトリ / S3 プレフィックス作成 |
| **d** / F8 | 削除 |
| **!** | 選択ファイルを引数にコマンド実行 |
| **x** | 実行ファイル / スクリプト実行 |
| **a** | アーカイブを開く |
| **o** / **Ctrl+O** | 一時サブシェル（親シェルを引き継ぎ） |
| **p** / F9 | AWS プロファイル |
| **t** / F10 | パス閲覧・ジャンプ |
| **u** | 設定 |
| **g** / Ctrl+G | パス直指定 |
| **Ctrl+L** | ペインをローカルへ |
| **s** / Ctrl+S | ペインを S3 へ |
| **f** / Ctrl+R | 再読込 |

---

## 4. 設定画面（`u`）

設定は自動で設定ファイルに保存されます。

| OS | パス |
|----|------|
| Windows | `%APPDATA%\s3filer\config.json` |
| Linux / macOS / WSL | `~/.config/s3filer/config.json` |

### 4.1 言語 (Language)

- **日本語** / **English**
- メッセージ・設定 UI・**ヘルプ本文**・ファンクションバー等が切り替わります
- 環境変数 `S3FILER_LANG=en` でも上書き可能

### 4.2 カラーテーマ

- アプリ全体の配色（ペイン、ダイアログ、View 背景、シンタックステーマ連動）
- 一覧で j/k するとプレビュー、Enter で確定保存
- CLI: `./sss --theme amber-crt --save-theme`
- テーマ変更は設定画面から（ステータスバーにテーマ用キーは表示しません）

### 4.3 ファイル表示 (View)

| モード | 動作 |
|--------|------|
| **組み込みビューア** | テキストはハイライト、画像は SIXEL（条件付き）、その他は HEX 等 |
| **拡張子ごとに外部コマンドを優先** | 設定した拡張子は外部コマンドで開く。未設定は組み込み |

#### 外部ビューアコマンド

設定の **「外部ビューアコマンド一覧」** で登録・編集・削除できます。

- `{}` がファイルパスに置換（省略時はコマンド末尾にパスを付与）
- 登録があると View (`v`) で **常にそのコマンドを使用**
- 起動は **バックグラウンド**（ファイラー画面は消えない。結果はメッセージ行）
- Git Bash 等ではパスをシェル向けに変換（例: `C:\Users\...` → `/c/Users/...`）
- Windows の `.CMD` ラッパー（`mdview` 等）もシェル経由で解決

| 拡張子 | コマンド例 (Windows) | コマンド例 (Linux) |
|--------|----------------------|---------------------|
| `.pdf` | `cmd /c start "" {}` | `xdg-open {}` |
| `.png` | `cmd /c start "" {}` | `xdg-open {}` |
| `.xlsx` | `cmd /c start "" {}` | `libreoffice {}` |

#### 設定ファイルをエディタで開く

パス表示だけでなく、`$EDITOR` / OS 既定エディタで `config.json` を開き、保存後に再読込します。

### 4.4 アーカイブ展開

| モード | 動作 |
|--------|------|
| **構造を維持** (`preserve`) | zip 内の `dir/a.txt` → `展開先/dir/a.txt` |
| **フラット** (`flat`) | `dir/a.txt` → `展開先/a.txt`（同名は `_1` 等を付与） |

展開先は、可能な場合 **反対側のローカルペイン** です。

### 4.5 SIXEL 画像表示

View (`v`) で画像を **端末グラフィックス（SIXEL）** で表示するかどうか。

| 選択肢 | 動作 |
|--------|------|
| **自動 (対応端末のみ)** | Windows Terminal / WezTerm 等を推定して有効化（既定） |
| **強制 ON** | 可能な限り SIXEL 表示を試行 |
| **強制 OFF** | 常にテキスト/HEX 等の組み込み表示 |

- 対応例: **Windows Terminal**（比較的新しい版）、WezTerm、mintty、mlterm、foot、Contour
- 環境変数でも上書き: `S3FILER_SIXEL=1`（ON）/ `0`（OFF）。設定より優先
- 要 **Pillow**（`requirements.txt` に含む。起動時や表示時に不足ならインストールを試行）
- 表示中は一時的に TUI を外し、**何かキー**でファイラーに戻る
- 端末サイズに合わせてリサイズ（アスペクト比維持、1:1 画素指定）

`config.json` のキー: `"sixel_mode": "auto" | "on" | "off"`

---

## 5. 宛先ブラウザ（`C` / `M` / `t`）

全ツリー一括ではなく、**現在のディレクトリ直下**だけを表示します。

| キー | 動作 |
|------|------|
| Enter / l | ディレクトリを開く |
| h / Backspace | 親へ |
| j / k | 移動 |
| 1 / Local ボタン | ローカル |
| 2 / S3 ボタン | S3 |
| / | フィルタ（インクリメンタルサーチ） |
| **s** / **g** | **今いる場所**を確定 |
| Esc | キャンセル |

- **c/m**: 反対ペインへ（クラシック）
- **C/M**: 宛先ブラウザで任意パス

---

## 6. View / 編集 / 実行 / シェル

### View（`v` / F3）

優先順位の目安:

1. 拡張子に **外部ビューア**が登録されている → 外部コマンド（バックグラウンド）
2. 画像 + SIXEL 有効 → **SIXEL 表示**
3. それ以外 → 組み込み（テキスト ハイライト / バイナリ HEX 等）

- View 内 **e**: `$VISUAL` / `$EDITOR` で編集（S3 は一時 DL → 変更時のみ upload）
- テキスト View の読込は先頭約 512KB。画像 SIXEL はより大きなサイズまで可（上限あり）

### コマンド（`!`）

- 選択ファイルを引数に実行（親シェル経由）
- `python {}` のように `{}` でファイル一覧に置換
- `{}` が無い場合はコマンド末尾にパスを付与
- パスはシェル種別に合わせてクォート / Git Bash 形式に変換

### 実行（`x` / Enter）

- `.sh` `.bat` `.ps1` `.py` `.exe` 等
- 確認ダイアログの後、TUI を一時停止して実行

### サブシェル（`o` / Ctrl+O）

- 対話シェルを起動（`exit` で復帰）
- cwd はアクティブなローカルパス優先
- **親プロセスのシェルを引き継ぎ**（PowerShell から起動 → pwsh、Git Bash → bash 等）
- 上書き: `S3FILER_SHELL`（例: `pwsh -NoLogo`）
- 便利な環境変数: `S3FILER_LEFT` / `RIGHT` / `ACTIVE` / `CWD` / `PROFILE`

---

## 7. アーカイブ（`a`）

対応: zip / jar / tar / tar.gz / tgz / tar.bz2 / tar.xz / gz / bz2 / xz など

| キー | 動作 |
|------|------|
| j/k | 移動 |
| Space | 選択 |
| v / Enter | メンバーを View |
| **x** / **e** | 選択（またはカーソル位置）を展開 |
| **a** | 全展開 |
| Esc | 閉じる（結果をステータスに表示） |

展開中は進捗ダイアログ（n/N）、完了時は通知とステータスメッセージを表示します。

---

## 8. コピー / 移動 / 削除

- 実行中: **進捗ダイアログ**（例: `Copy 2/5: name`）
- 完了: ステータスバー + トースト通知
- 大きな転送や S3 操作は時間がかかることがあります

---

## 9. ディレクトリ作成（`n` / F7）

- **ローカル**: 通常のフォルダ作成。`a/b/c` のような入れ子可
- **S3**: ゼロバイトのフォルダマーカー（末尾 `/`）を作成
  - バケット一覧（`s3://`）では作成不可 → バケットを開いてから実行
  - `logs/2026` のようなネストしたプレフィックス可

---

## 10. パス消失時のリカバリ

次のような場合、自動で親や既定位置にフォールバックします。

- 表示中のローカルフォルダが削除された
- プロファイル変更でバケット / プレフィックスが見えなくなった

| キー | 手動リカバリ |
|------|----------------|
| **f** | 再読込（リカバリ付き） |
| **Ctrl+L** | ローカル (cwd) |
| **s** / **Ctrl+S** | S3 ルート |
| **g** | 任意パスへ |

---

## 11. エラー時の対応

| 症状 | 確認・対処 |
|------|------------|
| S3 に繋がらない / ExpiredToken | `aws sso login --profile …` またはキー更新。プロファイル **p** |
| バケットが見えない | プロファイルとリージョン、IAM 権限 |
| 一覧が空で操作できない | **f** 再読込、**Ctrl+L** / **s**、**g** で移動 |
| 展開したのに見つからない | 展開先は **反対ペイン**優先。ステータスのパスを確認 |
| 外部ビューアが動かない | 設定 **u** でコマンドと `{}`。Git Bash ならパス変換を確認。PATH 上の実行ファイル |
| 画像がバイナリ表示になる | **u** → SIXEL を強制 ON。Windows Terminal 等か確認。Pillow を **同じ Python** に導入 |
| Pillow が必要と出る | `python -m pip install pillow`（`pip3` ではなく起動に使っている Python） |
| Windows で起動できない | PowerShell は **`.\sss.ps1`**。cmd は `.\sss.cmd` |
| PowerShell で `.\sss` が失敗する | 拡張子なし `sss` は bash 用。**`.\sss.ps1`** を使う |
| Python が見つからない | `python3` / `python`、venv 作成 |
| テーマが変わらない | **u** → カラーテーマ。設定ファイルの `theme` を確認 |

---

## 12. 注意事項

- **バケット自体の削除はしません**（オブジェクト / プレフィックス単位）
- テキスト View は大きなファイルの先頭のみ（約 512KB）
- S3 の編集・実行・アーカイブは一時ダウンロード（サイズ上限あり）
- 文字コードは推定（chardet）。確信度は Info 画面で確認
- 本番アカウント操作は十分注意し、可能なら読み取り専用プロファイルで確認

---

## 13. 関連ファイル

| ファイル | 役割 |
|----------|------|
| `sss` / `sss.ps1` / `sss.cmd` | 統一起動（bash / PowerShell / cmd） |
| `s3filer.sh` / `s3filer.ps1` | プラットフォーム別ラッパー |
| `s3filer/` | Python パッケージ（`sixel_view.py` 等） |
| 設定 `config.json` | テーマ・言語・ビューア・展開モード・`sixel_mode` 等 |

---

## 14. ライセンス

MIT
