# 更新履歴

バージョンは **コミット単位** で付けます。

| 変更の大きさ | 上げ方 | 例 |
|---|---|---|
| 大きめ | 0.01 単位を切り捨てて **0.1** 上げる | 0.2.1 → **0.3.0** |
| 小さめ | **0.01** 上げる | 0.2.0 → **0.2.1** |

マージコミットは中身が先行コミットと同じため、番号は付けません。

---

## 0.3.0 — 2026-08-19

大きめ（0.2.1 の 0.01 を切り捨てて 0.1 上げる）。

- 起動時に S3 のバケット一覧（`list_buckets`）を待たず、ローカルペインを先に描画する
- `boto3` の import を Textual の読み込みと重ね、S3 接続はバックグラウンドで行う
- ランチャーが毎回 `import s3filer` / Pillow のために Python を追加起動していたのをやめた
- 停止中の WSL UNC や切断されたネットワークドライブで場所一覧（`\`）が固まりにくくした
- `sss` がインストール先へ `cd` していたのをやめ、起動したディレクトリを左ペインの初期位置にする

対応: 起動高速化、起動ディレクトリの維持、HISTORY.md / バージョン 0.3.0

---

## 0.2.1 — 2026-08-18

小さめ。

- ビューアの検索で Enter 確定後、`n` / `N` が次・前のヒットに移動しない不具合を修正
- 検索ボックスを離してから `n` / `N` がビューアに届くようにした（App 側の `n`＝mkdir を継承しない）

対応: `15b1a23` Fix viewer n/N not moving between search hits.

---

## 0.2.0 — 2026-08-18

大きめ（0.1.0 から 0.1 上げる）。1 コミットに複数機能が含まれるが、番号はコミット単位で 1 つ。

- Windows の「この PC」一覧（`\`）: ドライブ、Box / OneDrive などクラウドフォルダ、WSL UNC
- 組み込みビューアで CSV / TSV を表表示（`t` で切り替え）
- ビューア内検索（`/` で検索、`n` / `N` で次・前）
- README / MANUAL（日英）を更新

対応: `446277c` Add Windows places switching, CSV/TSV tables, and viewer search.  
（`db84cf4` Merge pull request #1 は同じ内容のため番号なし）

---

## 0.1.0 — 2026-08-09

初回リリース。

- ローカルと Amazon S3 の 2 ペイン・コンソールファイラー
- コピー / 移動 / 削除 / 改名 / ディレクトリ作成
- 設定（言語・テーマ・外部ビューア・アーカイブ・SIXEL）
- テキストビューア（シンタックスハイライト）、画像の SIXEL 表示
- 外部エディタ、アーカイブ、コマンド実行、サブシェル
- 起動コマンド `sss`（Windows / Linux / macOS / Git Bash）
- ドキュメント（README / MANUAL、日英）

対応: `76f590a` Initial commit: S3 Filer dual-pane console file manager.
